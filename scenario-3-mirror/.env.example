# Ngrok Configuration
# Get your authtoken from: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=your_ngrok_authtoken_here

# Note: Free ngrok tier allows only 1 tunnel
# Both primary and mirror instances run, but only one ngrok tunnel is active
# Tunnel points to primary by default (port 3000)
# Access mirror locally at http://localhost:3001
